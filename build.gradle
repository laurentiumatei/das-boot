version '0.0.1-SNAPSHOT'
group = 'com.boot'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
	 compile(
	 "org.springframework.boot:spring-boot-starter-web:1.5.10.RELEASE",
	 'org.springframework.boot:spring-boot-devtools:1.5.10.RELEASE'
	 )
}
